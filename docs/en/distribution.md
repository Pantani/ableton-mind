# Distribution

See [Portuguese version](../distribution) for the canonical breakdown. English translation pending.

## Channels

1. **Claude Desktop `.mcpb`** — one-click bundle, primary path.
2. **npm** — `npm i -g ableton-mind`, published with provenance.
3. **Docker** — `ghcr.io/pantani/ableton-mind:latest`, CI/sandbox.
4. **Smithery** — listing for MCP discovery.

CI in `.github/workflows/` builds DXT + npm + Docker on tag.
