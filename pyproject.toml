# Intentionally empty: Ruff configuration lives in ruff.toml (single source of
# truth for the Python Remote Script under live/AbletonMind). This file exists
# so tools that probe for pyproject.toml (e.g. uv) don't error.
